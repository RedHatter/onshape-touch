{
  "name": "onshape-touch",
  "version": "1.0.0",
  "description": "Enable touch support for OnShape",
  "main": "onshape-touch.user.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "format": "prettier -w *.js *.ts *.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Ava Johnson",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.0.11",
    "prettier": "^3.3.2",
    "typescript": "^5.5.3"
  }
}
